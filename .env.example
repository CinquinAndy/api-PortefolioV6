HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
JWT_SECRET=tobemodified

# ISR Revalidation Configuration
# URL of your Next.js frontend (primary domain)
NEXT_PUBLIC_URL=https://andy-cinquin.com
# Alternate URL (optional, for multi-domain setups)
NEXT_PUBLIC_URL_ALT=https://andycinquin.fr
# Secret key for ISR revalidation (must match REVALIDATION_SECRET in Next.js .env)
# Generate with: openssl rand -base64 32
REVALIDATION_SECRET=your-secret-here
# Enable/disable automatic ISR revalidation (default: true)
ENABLE_ISR_REVALIDATION=true
